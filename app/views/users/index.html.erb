<% content_for(:sidebar) do %>
  <%= link_to 'New User', new_user_path, class: "btn btn-success btn-block"%>
<% end %>

<h1>
  <%= fa_icon(:male) %>
  Users
  <%= fa_icon(:female) %>
</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
    </tr>
  </thead>

  <tbody>
    <%= render @users %>
    <%= paginate @users %>
  </tbody>
</table>
